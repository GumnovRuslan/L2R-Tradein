import Layout from "@/components/Layout";
import CatalogPage from "@/containers/Catalog";

const CatalogPageSSR = () => {
  return (
    <Layout>
      <CatalogPage/>
    </Layout>
  )
}

export default CatalogPageSSR
